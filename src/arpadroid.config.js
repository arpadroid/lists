/** @type {import('@arpadroid/module').BuildConfigType} */
const config = {
    buildTypes: true,
    buildType: 'uiComponent',
    buildManifest: false,
    storybook_port: 6010,
    turbo: false
};

export default config;
